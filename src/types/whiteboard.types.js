/**
 * @typedef {{x:number, y:number}} Point
 * @typedef {{id:string, tool:string, color:string, width:number, points:Point[], text?:string}} Stroke
 * @typedef {{id:string, x:number, y:number, w:number, h:number, text:string, color:string}} StickyNoteModel
 */

export {};
